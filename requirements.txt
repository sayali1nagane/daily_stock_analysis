# Core dependencies for daily stock analysis
akshare>=1.10.0
pandas>=1.5.0
numpy>=1.23.0
matplotlib>=3.6.0
seaborn>=0.12.0
requests>=2.28.0
python-dotenv>=0.21.0
schedule>=1.1.0
loguru>=0.6.0
openpyxl>=3.0.10

# Data visualization
plotly>=5.11.0
kaleido>=0.2.1

# Notifications
# Note: smtplib is built into Python's standard library; smtplib2 is a thin wrapper
# Removing smtplib2 since I'm using the standard smtplib directly in my fork
# smtplib2>=0.2.1

# Testing
pytest>=7.2.0
pytest-cov>=4.0.0

# Linting and formatting
black>=22.10.0
flake8>=5.0.4
isort>=5.10.1

# Personal additions
# rich: nicer terminal output for debugging and manual runs
rich>=12.6.0
# tabulate: pretty-print DataFrames as tables in the terminal
tabulate>=0.9.0
# tqdm: progress bars when iterating over large stock lists locally
tqdm>=4.64.0
# ipython: convenient for interactive exploration in the REPL
ipython>=8.7.0
